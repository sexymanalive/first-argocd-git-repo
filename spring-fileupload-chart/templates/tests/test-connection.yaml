apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-fileupload-chart.fullname" . }}-test-connection"
  labels:
    {{- include "spring-fileupload-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-fileupload-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
