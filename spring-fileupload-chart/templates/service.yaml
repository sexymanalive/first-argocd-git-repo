apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-fileupload-chart.fullname" . }}
  labels:
    {{- include "spring-fileupload-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-fileupload-chart.selectorLabels" . | nindent 4 }}
